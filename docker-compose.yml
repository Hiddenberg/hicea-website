services:
  hicea-web:
    build:
      context: ./application
      platforms:
        - linux/amd64
        - linux/arm64
    image: hiddenberg/hicea-web:latest
    restart: always
    networks:
      - proxynetwork

networks:
  proxynetwork:
    external: true